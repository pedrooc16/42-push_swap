#include "./includes/push_swap.h"

int main(int ac, char **av)
{
	node	*stacks[2];
	if (!(count_args(av, ac))){
        ft_free_array(args, ac);
		write(1, "error\n", 6);
			return (0);
		}
        
	return 1;
}